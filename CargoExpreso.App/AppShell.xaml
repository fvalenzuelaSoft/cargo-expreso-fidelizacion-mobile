<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:CargoExpreso.App.Views"
       x:Class="CargoExpreso.App.AppShell"
       Shell.NavBarIsVisible="False"
       Shell.TabBarIsVisible="False">

    <!-- Auth pages (full screen, no tabs) -->
    <ShellContent Route="Login"
                  ContentTemplate="{DataTemplate views:LoginPage}"
                  Shell.NavBarIsVisible="False"
                  Shell.TabBarIsVisible="False"/>

    <ShellContent Route="Registro"
                  ContentTemplate="{DataTemplate views:RegistroPage}"
                  Shell.NavBarIsVisible="False"
                  Shell.TabBarIsVisible="False"/>

    <!-- Main app (Dashboard as root) -->
    <ShellContent Route="Dashboard"
                  ContentTemplate="{DataTemplate views:DashboardPage}"
                  Shell.NavBarIsVisible="False"
                  Shell.TabBarIsVisible="False"/>

</Shell>
